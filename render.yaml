services:
  - type: web
    name: qp-builder
    runtime: python
    buildCommand: pip install -r qp-builder/requirements.txt && cd qp-builder/frontend && npm install && npm run build
    startCommand: cd qp-builder && gunicorn --bind 0.0.0.0:$PORT server:app
